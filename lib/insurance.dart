import 'package:flutter/material.dart';

class InsurancePage extends StatelessWidget {
  static const routeName = '/insuPage';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Insurance Page"),
    );
  }
}
