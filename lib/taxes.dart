import 'package:flutter/material.dart';

class TaxPage extends StatelessWidget {
  static const routeName = '/taxPage';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Tax Page"),
    );
  }
}
